<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.workit.domain.reservation.mapper.ReservationMapper">

    <!-- 예약 생성 대상 워케이션 조회 결과 매핑 -->
    <resultMap id="reservationCreateWorkationMap"
               type="com.workit.domain.reservation.vo.ReservationCreateWorkationVO">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="regionId" column="region_id"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
    </resultMap>

    <!-- 예약 상품과 가맹점 검증 정보 조회 결과 매핑 -->
    <resultMap id="reservationCreateProductMap"
               type="com.workit.domain.reservation.vo.ReservationCreateProductVO">
        <id property="id" column="id"/>
        <result property="productName" column="product_name"/>
        <result property="productDetailType" column="product_detail_type"/>
        <result property="maxHeadcount" column="max_headcount"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="merchantName" column="merchant_name"/>
        <result property="merchantCategory" column="merchant_category"/>
        <result property="merchantRegionId" column="merchant_region_id"/>
    </resultMap>

    <!-- 예약 기간의 날짜별 재고 조회 결과 매핑 -->
    <resultMap id="reservationDailyInventoryMap"
               type="com.workit.domain.reservation.vo.ReservationDailyInventoryVO">
        <id property="id" column="id"/>
        <result property="inventoryDate" column="inventory_date"/>
        <result property="remainingCapacity" column="remaining_capacity"/>
        <result property="available" column="is_available"/>
    </resultMap>

    <!-- 예약 취소 검증과 결제 환불 대상 조회 결과 매핑 -->
    <resultMap id="reservationCancelTargetMap"
               type="com.workit.domain.reservation.vo.ReservationCancelTargetVO">
        <id property="reservationId" column="reservation_id"/>
        <result property="reservationCode" column="reservation_code"/>
        <result property="startDate" column="start_date"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="paymentTransactionId" column="payment_transaction_id"/>
    </resultMap>

    <!-- 예약 취소 시 복구할 날짜별 재고 조회 결과 매핑 -->
    <resultMap id="reservationCancelInventoryMap"
               type="com.workit.domain.reservation.vo.ReservationCancelInventoryVO">
        <id property="dailyInventoryId" column="daily_inventory_id"/>
        <result property="reservedCount" column="reserved_count"/>
    </resultMap>

    <!-- 예약 생성 중 워케이션 상태 변경과 중복 처리를 막기 위한 잠금 조회 -->
    <select id="selectWorkationForUpdate" resultMap="reservationCreateWorkationMap">
        SELECT id, user_id, region_id, start_date, end_date
        FROM workations
        WHERE id = #{workationId}
          AND user_id = #{userId}
          AND status = 'ACTIVE'
        FOR UPDATE
    </select>

    <!-- 결제 금액 산정 중 상품 정보 변경을 막기 위한 잠금 조회 -->
    <select id="selectReservationProductForUpdate" resultMap="reservationCreateProductMap">
        SELECT
            rp.id,
            rp.product_name,
            rp.product_detail_type,
            rp.max_headcount,
            rp.unit_price,
            m.id AS merchant_id,
            m.name AS merchant_name,
            m.category AS merchant_category,
            m.region_id AS merchant_region_id
        FROM reservation_products rp
        JOIN merchants m ON m.id = rp.merchant_id
        WHERE rp.id = #{productId}
        FOR UPDATE
    </select>

    <!-- 숙소는 종료일 미포함, 오피스는 종료일 포함 기준의 동일 카테고리 예약 중복 검사 -->
    <select id="countOverlappingReservation" resultType="int">
        SELECT COUNT(*)
        FROM reservations r
        JOIN reservation_products rp ON rp.id = r.product_id
        JOIN merchants m ON m.id = rp.merchant_id
        WHERE r.user_id = #{userId}
          AND r.workation_id = #{workationId}
          AND r.status IN ('CONFIRMED', 'COMPLETED')
          AND m.category = #{category}
        <choose>
            <when test="includeEndDate">
                AND r.start_date &lt;= #{endDate}
                AND r.end_date &gt;= #{startDate}
            </when>
            <otherwise>
                AND r.start_date &lt; #{endDate}
                AND r.end_date &gt; #{startDate}
            </otherwise>
        </choose>
    </select>

    <!-- 상품 유형별 종료일 포함 여부를 적용한 날짜별 재고의 날짜순 잠금 조회 -->
    <select id="selectDailyInventoriesForUpdate" resultMap="reservationDailyInventoryMap">
        SELECT id, inventory_date, remaining_capacity, is_available
        FROM product_daily_inventories
        WHERE product_id = #{productId}
          AND inventory_date &gt;= #{startDate}
        <choose>
            <when test="includeEndDate">
                AND inventory_date &lt;= #{endDate}
            </when>
            <otherwise>
                AND inventory_date &lt; #{endDate}
            </otherwise>
        </choose>
        ORDER BY inventory_date ASC
        FOR UPDATE
    </select>

    <!-- 예약 상품 존재 여부와 날짜 범위 규칙 확인을 위한 세부 유형 조회 -->
    <select id="selectReservationProductDetailType"
            resultType="com.workit.domain.reservation.vo.ReservationProductDetailType">
        SELECT product_detail_type
        FROM reservation_products
        WHERE id = #{productId}
    </select>

    <!-- 예약 상품의 날짜별 재고 읽기 전용 조회 -->
    <select id="selectDailyInventories" resultMap="reservationDailyInventoryMap">
        SELECT id, inventory_date, remaining_capacity, is_available
        FROM product_daily_inventories
        WHERE product_id = #{productId}
          AND inventory_date &gt;= #{startDate}
        <choose>
            <when test="includeEndDate">
                AND inventory_date &lt;= #{endDate}
            </when>
            <otherwise>
                AND inventory_date &lt; #{endDate}
            </otherwise>
        </choose>
        ORDER BY inventory_date ASC
    </select>

    <!-- 결제와 재고 검증을 통과한 확정 예약 저장 -->
    <insert id="insertReservation"
            parameterType="com.workit.domain.reservation.vo.ReservationCreateVO"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO reservations (
            user_id,
            workation_id,
            product_id,
            reservation_code,
            start_date,
            end_date,
            headcount,
            quantity,
            total_amount,
            status,
            created_at
        ) VALUES (
            #{userId},
            #{workationId},
            #{productId},
            #{reservationCode},
            #{startDate},
            #{endDate},
            #{headcount},
            #{quantity},
            #{totalAmount},
            #{status},
            NOW()
        )
    </insert>

    <!-- 생성된 예약 PK를 조합한 최종 예약번호 반영 -->
    <update id="updateReservationCode">
        UPDATE reservations
        SET reservation_code = #{reservationCode}
        WHERE id = #{reservationId}
    </update>

    <!-- 판매 가능 상태와 잔여 수량을 함께 검증하는 조건부 재고 차감 -->
    <update id="updateDailyInventory">
        UPDATE product_daily_inventories
        SET remaining_capacity = remaining_capacity - #{reservedCount}
        WHERE id = #{dailyInventoryId}
          AND is_available = 1
          AND remaining_capacity &gt;= #{reservedCount}
    </update>

    <!-- 예약 취소 시 동일 수량을 복구하기 위한 예약별 재고 차감 이력 저장 -->
    <insert id="insertReservationDailyInventory">
        INSERT INTO reservation_daily_inventories (
            reservation_id,
            daily_inventory_id,
            reserved_count
        ) VALUES (
            #{reservationId},
            #{dailyInventoryId},
            #{reservedCount}
        )
    </insert>

    <!-- 숙소는 퇴실일, 오피스는 이용 종료일 다음 날 기준의 완료 상태 일괄 변경 -->
    <update id="updateCompletedReservationStatuses">
        UPDATE reservations r
        JOIN reservation_products rp ON rp.id = r.product_id
        JOIN merchants m ON m.id = rp.merchant_id
        SET r.status = 'COMPLETED'
        WHERE r.status = 'CONFIRMED'
          AND (
            (m.category = 'ACCOMMODATION' AND r.end_date &lt;= #{today})
            OR (m.category = 'OFFICE' AND r.end_date &lt; #{today})
          )
    </update>

    <!-- 동일 예약의 중복 취소를 직렬화하고 최신 정상 결제 건을 찾기 위한 잠금 조회 -->
    <select id="selectReservationCancelTargetForUpdate"
            resultMap="reservationCancelTargetMap">
        SELECT
            r.id AS reservation_id,
            r.reservation_code,
            r.start_date,
            r.total_amount,
            r.status,
            (
                SELECT t.id
                FROM transactions t
                WHERE t.reservation_id = r.id
                  AND t.user_id = r.user_id
                  AND t.transaction_type = 'PAYMENT'
                  AND t.status = 'PAID'
                ORDER BY t.approved_at DESC, t.id DESC
                LIMIT 1
            ) AS payment_transaction_id
        FROM reservations r
        WHERE r.id = #{reservationId}
          AND r.user_id = #{userId}
        FOR UPDATE
    </select>

    <!-- 교착 상태 가능성을 줄이기 위해 재고 ID 순서로 고정한 복구 대상 잠금 조회 -->
    <select id="selectReservationCancelInventoriesForUpdate"
            resultMap="reservationCancelInventoryMap">
        SELECT
            rdi.daily_inventory_id,
            rdi.reserved_count
        FROM reservation_daily_inventories rdi
        JOIN product_daily_inventories pdi ON pdi.id = rdi.daily_inventory_id
        WHERE rdi.reservation_id = #{reservationId}
        ORDER BY rdi.daily_inventory_id ASC
        FOR UPDATE
    </select>

    <!-- 총 재고를 초과하지 않는 범위의 조건부 재고 복구 -->
    <update id="updateDailyInventoryForCancellation">
        UPDATE product_daily_inventories
        SET remaining_capacity = remaining_capacity + #{reservedCount}
        WHERE id = #{dailyInventoryId}
          AND remaining_capacity + #{reservedCount} &lt;= total_capacity
    </update>

    <!-- 취소 수수료와 환불 금액 및 처리 시각 이력 저장 -->
    <insert id="insertReservationCancel"
            parameterType="com.workit.domain.reservation.vo.ReservationCancelVO">
        INSERT INTO reservation_cancels (
            reservation_id,
            cancel_fee,
            refund_amount,
            canceled_at,
            refunded_at
        ) VALUES (
            #{reservationId},
            #{cancelFee},
            #{refundAmount},
            #{canceledAt},
            #{refundedAt}
        )
    </insert>

    <!-- 동시 상태 변경을 방어하기 위한 확정 예약의 조건부 취소 상태 변경 -->
    <update id="updateReservationStatusToCanceled">
        UPDATE reservations
        SET status = 'CANCELED'
        WHERE id = #{reservationId}
          AND status = 'CONFIRMED'
    </update>

    <!-- 예약 목록 항목 조회 결과 매핑 -->
    <resultMap id="reservationListItemMap"
               type="com.workit.domain.reservation.vo.ReservationListItemVO">
        <id property="reservationId" column="reservation_id"/>
        <result property="reservationCode" column="reservation_code"/>
        <result property="merchantName" column="merchant_name"/>
        <result property="productName" column="product_name"/>
        <result property="productDetailType" column="product_detail_type"/>
        <result property="thumbnailUrl" column="thumbnail_url"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="headcount" column="headcount"/>
        <result property="quantity" column="quantity"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="canceledAt" column="canceled_at"/>
        <result property="refundAmount" column="refund_amount"/>
    </resultMap>

    <!-- 예약 확정과 이용 완료 상세 조회 결과 매핑 -->
    <resultMap id="reservationDetailMap"
               type="com.workit.domain.reservation.vo.ReservationDetailVO">
        <id property="reservationId" column="reservation_id"/>
        <result property="reservationCode" column="reservation_code"/>
        <result property="workationId" column="workation_id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="merchantName" column="merchant_name"/>
        <result property="merchantCategory" column="merchant_category"/>
        <result property="merchantAddress" column="merchant_address"/>
        <result property="merchantPhoneNumber" column="merchant_phone_number"/>
        <result property="merchantThumbnailUrl" column="merchant_thumbnail_url"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="productDetailType" column="product_detail_type"/>
        <result property="productThumbnailUrl" column="product_thumbnail_url"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="headcount" column="headcount"/>
        <result property="quantity" column="quantity"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="paymentMethod" column="payment_method"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="reviewId" column="review_id"/>
        <result property="reviewStatus" column="review_status"/>
    </resultMap>

    <!-- 예약 취소 상세 조회 결과 매핑 -->
    <resultMap id="reservationCancellationDetailMap"
               type="com.workit.domain.reservation.vo.ReservationCancellationDetailVO">
        <id property="reservationId" column="reservation_id"/>
        <result property="reservationCode" column="reservation_code"/>
        <result property="merchantName" column="merchant_name"/>
        <result property="productName" column="product_name"/>
        <result property="productDetailType" column="product_detail_type"/>
        <result property="thumbnailUrl" column="thumbnail_url"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="canceledAt" column="canceled_at"/>
        <result property="cancelFee" column="cancel_fee"/>
        <result property="refundAmount" column="refund_amount"/>
    </resultMap>

    <sql id="reservationListFrom">
        <!-- 목록 화면에 필요한 예약 상품과 가맹점 정보 조인 -->
        FROM reservations r
        JOIN reservation_products rp ON rp.id = r.product_id
        JOIN merchants m ON m.id = rp.merchant_id
    </sql>

    <sql id="reservationListWhere">
        <!-- 로그인 사용자 소유 예약으로 제한하고 전달받은 모든 상태를 IN 조건으로 조회 -->
        WHERE r.user_id = #{userId}
        <if test="workationId != null">
          AND r.workation_id = #{workationId}
        </if>
          AND r.status IN
        <foreach collection="statuses" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        <if test="category != null">
          AND m.category = #{category}
        </if>
    </sql>

    <select id="selectReservationList" resultMap="reservationListItemMap">
        SELECT
            r.id AS reservation_id,
            r.reservation_code,
            m.name AS merchant_name,
            rp.product_name,
            rp.product_detail_type,
            rp.thumbnail_url,
            r.start_date,
            r.end_date,
            r.headcount,
            r.quantity,
            r.total_amount,
            r.status,
            CASE
                WHEN r.status = 'CANCELED' THEN rc.canceled_at
                ELSE NULL
            END AS canceled_at,
            CASE
                WHEN r.status = 'CANCELED' THEN rc.refund_amount
                ELSE NULL
            END AS refund_amount
        <include refid="reservationListFrom"/>
        <!-- 취소 이력이 여러 건이면 가장 최근 이력의 취소 일시와 최종 환불 금액을 함께 조회 -->
        LEFT JOIN reservation_cancels rc ON rc.id = (
            SELECT rc2.id
            FROM reservation_cancels rc2
            WHERE rc2.reservation_id = r.id
            ORDER BY rc2.canceled_at DESC, rc2.id DESC
            LIMIT 1
        )
        <include refid="reservationListWhere"/>
        <!-- 취소 탭과 예약/이용 내역 탭의 정렬 정책을 구분 -->
        <choose>
            <when test="canceledOnly">
                ORDER BY canceled_at DESC, reservation_id DESC
            </when>
            <otherwise>
                ORDER BY start_date ASC, reservation_id DESC
            </otherwise>
        </choose>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countReservationList" resultType="long">
        <!-- 페이지 메타데이터 계산을 위해 목록과 동일한 필터 조건의 전체 건수를 조회 -->
        SELECT COUNT(*)
        <include refid="reservationListFrom"/>
        <include refid="reservationListWhere"/>
    </select>

    <select id="selectReservationDetails" resultMap="reservationDetailMap">
        <!-- 로그인 사용자가 소유한 예약 확정·이용 완료 건만 상세 조회 -->
        SELECT
            r.id AS reservation_id,
            r.reservation_code,
            r.workation_id,
            m.id AS merchant_id,
            m.name AS merchant_name,
            m.category AS merchant_category,
            m.address AS merchant_address,
            m.phone_number AS merchant_phone_number,
            m.thumbnail_url AS merchant_thumbnail_url,
            rp.id AS product_id,
            rp.product_name,
            rp.product_detail_type,
            rp.thumbnail_url AS product_thumbnail_url,
            r.start_date,
            r.end_date,
            r.headcount,
            r.quantity,
            r.total_amount,
            (
                SELECT t.payment_source_type
                FROM transactions t
                WHERE t.reservation_id = r.id
                  AND t.transaction_type = 'PAYMENT'
                  AND t.status = 'PAID'
                ORDER BY t.approved_at DESC, t.id DESC
                LIMIT 1
            ) AS payment_method,
            r.status,
            r.created_at,
            rv.review_id,
            rv.status AS review_status
        FROM reservations r
        JOIN reservation_products rp ON rp.id = r.product_id
        JOIN merchants m ON m.id = rp.merchant_id
        LEFT JOIN reviews rv ON rv.reservation_id = r.id
        WHERE r.id = #{reservationId}
          AND r.user_id = #{userId}
          AND r.status IN ('CONFIRMED', 'COMPLETED')
    </select>

    <select id="selectReservationCancellationDetails"
            resultMap="reservationCancellationDetailMap">
        <!-- 로그인 사용자가 소유한 취소 예약만 상세 조회 -->
        SELECT
            r.id AS reservation_id,
            r.reservation_code,
            m.name AS merchant_name,
            rp.product_name,
            rp.product_detail_type,
            rp.thumbnail_url,
            r.start_date,
            r.end_date,
            r.total_amount,
            rc.canceled_at,
            rc.cancel_fee,
            rc.refund_amount
        FROM reservations r
        JOIN reservation_products rp ON rp.id = r.product_id
        JOIN merchants m ON m.id = rp.merchant_id
        JOIN reservation_cancels rc ON rc.reservation_id = r.id
        WHERE r.id = #{reservationId}
          AND r.user_id = #{userId}
          AND r.status = 'CANCELED'
    </select>

</mapper>
